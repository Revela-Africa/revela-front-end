mutation StartAssessment(
  $vehicleId: String!
  $notes: String!
  $photoUrls: [String!]!
) {
  startAssessment(
    vehicleId: $vehicleId
    notes: $notes
    photoUrls: $photoUrls
  ) {
    id
    status
  }
}